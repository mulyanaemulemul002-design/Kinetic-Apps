{
  "name": "kinetic-dao-contracts",
  "private": true,
  "version": "0.2.0",
  "type": "commonjs",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat compile && node ../scripts/deploy-all.mjs"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.6.1",
    "hardhat": "^2.22.0"
  }
}
